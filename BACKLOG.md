# Backlog

- **Policy scheduling** — time-window activation per policy (e.g. block during work hours, looser on weekends).
- **Sync history data** — sync `usage` across devices so daily caps actually count usage everywhere; needs additive merge semantics, not LWW.
- **Friction features (access gates)** — password prompt, wait-N-seconds delay, typing challenge. Placeholder card already in the policy view.
